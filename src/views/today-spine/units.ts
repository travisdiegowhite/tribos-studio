/**
 * Unit formatting for the Training-Arc Today — re-exported from the glance so
 * the two Today surfaces format mi/km identically.
 */

export type { UnitsPreference } from '../today-glance/units';
export { formatDistanceKm, formatElevationM, formatDurationMin } from '../today-glance/units';
