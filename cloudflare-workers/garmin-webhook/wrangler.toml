name = "garmin-webhook"
main = "src/index.js"
compatibility_date = "2025-01-15"

# Thin proxy: verifies signature, stores events to Supabase, returns 200.
# All processing happens via Vercel cron (api/garmin-webhook-process.js).

# Environment variables (set in Cloudflare dashboard):
# SUPABASE_URL - Your Supabase project URL
# SUPABASE_SERVICE_KEY - Your Supabase service_role key
# GARMIN_WEBHOOK_SECRET (optional) - Webhook signature verification secret

# Free plan (10ms CPU) is fine — this worker only does 1-2 Supabase calls per request.
